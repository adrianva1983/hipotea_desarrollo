{% if not is_granted('ROLE_CLIENTE') %}
	{% set crearnuevo = path('agregar_modificar_expediente') %}
	{% set crearnuevo_texto = 'Crear nuevo' %}
{% endif %}
{% extends '@App/Backoffice/base.html.twig' %}
{% block devstyles %}
	{{ parent() }}
	<link href="{{ asset('assets/node_modules/select2/dist/css/select2.min.css') }}" rel="stylesheet">
{% endblock %}
{% block cloudstyles %}
	{{ parent() }}
	<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.6-rc.1/css/select2.min.css" rel="stylesheet">
{% endblock %}
{% block customstyles %}
	{{ parent() }}
	<link href="{{ asset('assets/css/custom/select2.min.css') }}" rel="stylesheet">
	<link href="{{ asset('assets/css/custom/lista-expedientes.min.css') }}" rel="stylesheet">
	<style>
		#tabla-entidades {
			max-width: 100% !important;
		}

		#tabla-entidades thead th:nth-child(1),
		#tabla-entidades thead th:nth-child(2),
		#tabla-entidades thead th:nth-child(3),
		#tabla-entidades thead th:nth-child(4),
		#tabla-entidades thead th:nth-child(5),
		#tabla-entidades thead th:nth-child(6),
		#tabla-entidades thead th:nth-child(7),
		#tabla-entidades thead th:nth-child(8),
		#tabla-entidades thead th:nth-child(9),
		#tabla-entidades thead th:nth-child(10),
		#tabla-entidades thead th:nth-child(11),
		#tabla-entidades thead th:nth-child(12),
		#tabla-entidades thead th:nth-child(13),
		#tabla-entidades thead th:nth-child(14),
		#tabla-entidades thead th:nth-child(15),
		#tabla-entidades tbody tr td:nth-child(1),
		#tabla-entidades tbody tr td:nth-child(2),
		#tabla-entidades tbody tr td:nth-child(3),
		#tabla-entidades tbody tr td:nth-child(4),
		#tabla-entidades tbody tr td:nth-child(5),
		#tabla-entidades tbody tr td:nth-child(6),
		#tabla-entidades tbody tr td:nth-child(7),
		#tabla-entidades tbody tr td:nth-child(8),
		#tabla-entidades tbody tr td:nth-child(9),
		#tabla-entidades tbody tr td:nth-child(10),
		#tabla-entidades tbody tr td:nth-child(11),
		#tabla-entidades tbody tr td:nth-child(12),
		#tabla-entidades tbody tr td:nth-child(13),
		#tabla-entidades tbody tr td:nth-child(14),
		#tabla-entidades tbody tr td:nth-child(15) {
			max-width: max-content !important;
			width: max-content !important;
		}

		a.editar {
			border: 2px solid green;
			padding: 2px;
			border-radius: 5px;
			color: green;
			background: white;
			font-size: 18px;
		}
		a.documentos {
			border: 2px solid blue;
			padding: 2px 5px;
			border-radius: 5px;
			color: blue;
			background: white;
			font-size: 18px;
		}

		a.notificar,
		a.cancelar {
			border: 2px solid red;
			padding: 2px 5px;
			border-radius: 5px;
			color: red;
			background: white;
			font-size: 18px;
		}

		a.email {
			border: 2px solid gray;
			padding: 2px 5px;
			border-radius: 5px;
			color: gray;
			background: white;
			font-size: 18px;
		}
		a.informe {
			border: 2px solid orange;
			padding: 2px 10px;
			border-radius: 5px;
			color: orange;
			background: white;
			font-size: 18px;
		}

		a.contrato {
			border: 2px solid cornflowerblue;
			padding: 2px 10px;
			border-radius: 5px;
			color: cornflowerblue;
			background: white;
			font-size: 18px;
		}

		a.pendiente,
		a.seguimiento {
			border: 2px solid darkmagenta;
			padding: 2px 5px;
			border-radius: 5px;
			color: darkmagenta;
			background: white;
			font-size: 18px;
		}

		a.registro,
		a.firmas {
			border: 2px solid tomato;
			padding: 2px 5px;
			border-radius: 5px;
			color: tomato;
			background: white;
			font-size: 18px;
		}

		a.duplicar {
			border: 2px solid teal;
			padding: 2px 5px;
			border-radius: 5px;
			color: teal;
			background: white;
			font-size: 18px;
		}

		a.whatsapp {
			border: 2px solid #25d366;
			padding: 2px 5px;
			border-radius: 5px;
			color: #25d366 !important;
			background: white;
			font-size: 18px;
		}

		/* Icono WhatsApp: sesión activa pero sin mensajes */
		a.whatsapp.wa-session-no-messages {
			border: 2px solid #f2b01e;
			padding: 2px 5px;
			border-radius: 5px;
			color: #f2b01e !important;
			background: white;
			font-size: 18px;
		}

		a.editar span,
		a.documentos span,
		a.notificar span,
		a.email span,
		a.informe span,
		a.pendiente span,
		a.registro span,
		a.duplicar span,
		a.firmas span,
		a.seguimiento span,
		a.cancelar span,
		a.whatsapp span {
			display: none;
		}

		@media(max-width: 576px) {
			#tabla-entidades thead th,
			#tabla-entidades tbody tr td {
				max-width: max-content !important;
				width: max-content !important;
			}

			#tabla-entidades td:nth-child(1) {
				padding-right: 20px !important;
			}
		}
		
		.pagination-info {
			text-align: center;
			margin-top: 10px;
			color: #666;
		}
		
		.estado-btn {
			min-height: 32px !important;
		}
	</style>
{% endblock %}
{% block devscripts %}
	{{ parent() }}
	<script src="{{ asset('assets/node_modules/select2/dist/js/select2.full.min.js') }}"></script>
	<script src="{{ asset('assets/node_modules/select2/dist/js/i18n/es.js') }}"></script>
{% endblock %}
{% block cloudscripts %}
	{{ parent() }}
	<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.6-rc.1/js/select2.full.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.6-rc.1/js/i18n/es.js"></script>
{% endblock %}
{% block customscripts %}
	{{ parent() }}
	<script>
		$(function () {
			'use strict';
			
			// DEBUG: Contar opciones antes de inicializar Select2
			var countClientes = $('#clientes option').length;
			//console.log('Opciones de clientes en HTML:', countClientes);
			$('#count-clientes').text(countClientes);
			
			// IMPORTANTE: Destruir TODAS las instancias de Select2 antes de inicializar
			$('select').each(function() {
				if ($(this).data('select2')) {
					$(this).select2('destroy');
				}
			});
			
			// Esperar un momento antes de reinicializar
			setTimeout(function() {
				// Inicializar todos los select2
				$('.select2').each(function() {
					var placeholderText = $(this).data('placeholder') || '';
					var isMultiple = $(this).attr('multiple') !== undefined;
					
					//console.log('Inicializando select:', $(this).attr('id'), 'con', $(this).find('option').length, 'opciones, multiple:', isMultiple);
					
					$(this).select2({
						language: 'es',
						placeholder: placeholderText,
						allowClear: true,
						width: '100%',
						closeOnSelect: !isMultiple
					});
				});
				
				//console.log('Select2 inicializado correctamente');
			}, 100);
			
			$('[data-toggle="popover"]').popover();
			
			// Manejar selección de todos los checkboxes
			$('#seleccionar-todas-entidades').on('change', function() {
				$('input[name="expediente[]"]').prop('checked', $(this).prop('checked'));
			});
			
			// Si se desmarca alguno individualmente, desmarcar el "seleccionar todos"
			$(document).on('change', 'input[name="expediente[]"]', function() {
				if (!$(this).prop('checked')) {
					$('#seleccionar-todas-entidades').prop('checked', false);
				} else {
					// Si todos están marcados, marcar el "seleccionar todos"
					if ($('input[name="expediente[]"]:checked').length === $('input[name="expediente[]"]').length) {
						$('#seleccionar-todas-entidades').prop('checked', true);
					}
				}
			});
			
			// Funcionalidad de búsqueda global con debounce
			var searchTimeout;
			$('#buscar-tabla').on('keyup', function() {
				clearTimeout(searchTimeout);
				var valor = $(this).val();
				
				// Si el campo está vacío, no hacer búsqueda
				if (valor.length === 0) {
					return;
				}
				
				// Esperar 500ms después de que el usuario deje de escribir
				searchTimeout = setTimeout(function() {
					// Obtener la URL actual y agregar el parámetro de búsqueda
					var url = new URL(window.location.href);
					url.searchParams.set('buscar', valor);
					url.searchParams.set('page', '1'); // Resetear a la página 1
					
					// Redirigir a la URL con el parámetro de búsqueda
					window.location.href = url.toString();
				}, 500);
			});
			
			// Limpiar búsqueda si se borra el campo
			$('#buscar-tabla').on('search', function() {
				if ($(this).val() === '') {
					var url = new URL(window.location.href);
					url.searchParams.delete('buscar');
					window.location.href = url.toString();
				}
			});
		});

		function rutaDuplicarExpediente() {
			return '{{ path('duplicar_expediente',{'id':' '}) }}';
		}

		function rutaNotificacionExpediente() {
			return '{{ path('notificacion_expediente',{'id':' '}) }}';
		}

		function rutaCancelarExpediente() {
			return '{{ path('cancelar_expediente',{'id':' '}) }}';
		}
		
		function cambiarLimit(nuevoLimit) {
			// Obtener la URL actual con todos sus parámetros
			var url = new URL(window.location.href);
			
			// Actualizar el parámetro limit
			url.searchParams.set('limit', nuevoLimit);
			
			// Resetear a la página 1 al cambiar el límite
			url.searchParams.set('page', '1');
			
			// Redirigir a la nueva URL
			window.location.href = url.toString();
		}
		
		// Manejo de botones de estado (Cancelados, Activos, Firmados)
		$(function() {
			// Función para aplicar estilos al botón según el estado
			function aplicarEstiloBotón(estado) {
				//console.log('Aplicando estilo al estado:', estado);
				// Remover estilos de todos los botones
				$('.estado-btn').removeAttr('style').removeClass('active');
				
				// Buscar el botón con el estado y aplicar estilo
				var botón = $('.estado-btn[data-estado="' + estado + '"]');
				if (botón.length) {
					//console.log('Botón encontrado para estado:', estado);
					if (estado == '0') {
						botón.css({'background-color': '#dc3545', 'color': 'white'});
					} else if (estado == '1') {
						botón.css({'background-color': '#28a745', 'color': 'white'});
					} else if (estado == '2') {
						botón.css({'background-color': '#007bff', 'color': 'white'});
					} 
				}
			}
			
			// Al cargar la página, activar el botón correspondiente al valor del input oculto
			var estadoActual = $('#estado-filtro').val();
			//console.log('Estado actual del input oculto:', estadoActual);
			
			// Si no hay estado o está vacío, usar '1' (Activos) por defecto
			if (!estadoActual || estadoActual === '') {
				estadoActual = '1';
				$('#estado-filtro').val('1');
			}
			
			aplicarEstiloBotón(estadoActual);
			
			// Evento click en los botones
			$('.estado-btn').on('click', function() {
				var estado = $(this).data('estado');
				//console.log('Click en botón con estado:', estado);
				
				// Aplicar estilos al botón clickeado
				aplicarEstiloBotón(estado);
				
				// Actualizar el valor del input oculto
				$('#estado-filtro').val(estado);
			});
		});
	</script>
	<script>
		/**
		 * Detecta y obtiene la URI de imagen adecuada a partir del mensaje.
		 * Soporta URLs directas, data-URIs existentes y base64 puro (auto-detectando tipo mime).
		 */
		function normalizeWhatsappBase64(rawValue) {
			if (!rawValue || typeof rawValue !== 'string') {
				return null;
			}

			var normalizedValue = rawValue.trim();
			var dataUriMatch = normalizedValue.match(/^data:(image\/[a-zA-Z+.-]+);base64,(.+)$/i);
			if (dataUriMatch) {
				normalizedValue = dataUriMatch[2];
			}

			normalizedValue = normalizedValue.replace(/\s/g, '').replace(/-/g, '+').replace(/_/g, '/');

			var padding = normalizedValue.length % 4;
			if (padding) {
				normalizedValue += '='.repeat(4 - padding);
			}

			if (!/^[A-Za-z0-9+/]+={0,2}$/.test(normalizedValue)) {
				return null;
			}

			return normalizedValue;
		}

		function buildWhatsappImageDataUri(base64Content, preferredMime) {
			var cleanBase64 = normalizeWhatsappBase64(base64Content);
			if (!cleanBase64) {
				return null;
			}

			var mime = preferredMime && /^image\//i.test(preferredMime) ? preferredMime : null;
			if (!mime) {
				if (cleanBase64.indexOf('iVBORw0KGgo') === 0) {
					mime = 'image/png';
				} else if (cleanBase64.indexOf('/9j/') === 0) {
					mime = 'image/jpeg';
				} else if (cleanBase64.indexOf('R0lGOD') === 0) {
					mime = 'image/gif';
				} else if (cleanBase64.indexOf('UklGR') === 0) {
					mime = 'image/webp';
				}
			}

			if (!mime && cleanBase64.length > 50) {
				mime = 'image/jpeg';
			}

			return mime ? 'data:' + mime + ';base64,' + cleanBase64 : null;
		}

		function getWhatsappImageSrc(rawMessage, preferredMime) {
			if (!rawMessage) return null;
			rawMessage = rawMessage.trim();

			// 1. Si empieza por { intentar parsear como JSON (imágenes desde webhook)
			if (rawMessage.charAt(0) === '{') {
				try {
					var parsed = JSON.parse(rawMessage);
					if (parsed) {
						if (parsed.type === 'image' && parsed.filepath) {
							return parsed.filepath;
						}
						// Caso: {type: 'image', content: 'base64...', mime_type: 'image/png'}
						if (parsed.type === 'image' && parsed.content) {
							var parsedImageSrc = buildWhatsappImageDataUri(parsed.content, parsed.mime_type || preferredMime);
							if (parsedImageSrc) {
								return parsedImageSrc;
							}
						}
						if (parsed.type === 'image' && parsed.url) {
							return parsed.url;
						}
						// Intentar extraer contenido en otros formatos
						if (parsed.content) {
							return getWhatsappImageSrc(parsed.content, parsed.mime_type || preferredMime);
						}
						if (parsed.url) {
							return parsed.url;
						}
					}
				} catch(e) {}
			}

			// 2. Si empieza por http/https
			if (/^https?:\/\//i.test(rawMessage)) {
				return rawMessage;
			}

			if (/^\/uploads\//i.test(rawMessage)) {
				return rawMessage;
			}

			// 3. Si ya es un Data URI con prefijo
			if (/^data:image\/[a-zA-Z+.-]+;base64,/i.test(rawMessage)) {
				return rawMessage;
			}

			// 4. Si es base64 puro, detectar cabeceras y añadir tipo mime
			return buildWhatsappImageDataUri(rawMessage, preferredMime);
		}

		/**
		 * Construye el HTML de contenido de un mensaje WhatsApp.
		 * Soporta imágenes guardadas como JSON {type,content,mime_type} o URL pura.
		 * Auto-detecta JSON de imagen aunque message_type no venga en la respuesta.
		 */
		function buildWhatsappMessageContent(msg) {
			var rawMessage = msg.message || '';
			var type = msg.message_type || 'text';
			var IMG_FALLBACK = 'data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22200%22 height=%22150%22%3E%3Crect fill=%22%23ddd%22 width=%22200%22 height=%22150%22/%3E%3Ctext x=%2250%25%22 y=%2250%25%22 text-anchor=%22middle%22 dy=%22.3em%22 fill=%22%23999%22 font-size=%2213%22%3EImagen no disponible%3C/text%3E%3C/svg%3E';

			var parsedJSON = null;
			if (rawMessage.charAt(0) === '{') {
				try {
					parsedJSON = JSON.parse(rawMessage);
				} catch(e) {}
			}

			// Procesar JSON de imagen guardado desde el webhook
			if (parsedJSON && parsedJSON.type === 'image') {
				type = 'image';
				
				// Obtener el contenido base64
				var imgContent = parsedJSON.content || null;
				var mimeType = parsedJSON.mime_type || 'image/jpeg';
				var caption = parsedJSON.text || null;
				
				// Si hay contenido base64, crear data URI
				var imgSrc = getWhatsappImageSrc(rawMessage, mimeType) || IMG_FALLBACK;
				
				// Retornar imagen con caption opcional
				var html = '<img src="' + imgSrc + '" alt="Imagen" class="whatsapp-image" style="max-width:250px;max-height:300px;border-radius:12px;margin:4px 0;display:block;cursor:pointer;" onclick="window.open(this.src,\'_blank\')" onerror="this.src=\''+IMG_FALLBACK+'\';this.onclick=null;">';
				
				if (caption) {
					html += '<div class="whatsapp-caption" style="margin-top:4px;font-size:13px;color:rgba(0,0,0,0.7);">' + $('<div/>').text(caption).html() + '</div>';
				}
				
				return html;
			}

			// Si no es JSON válido, procesar como antes
			if (rawMessage.charAt(0) === '{') {
				try {
					parsedJSON = JSON.parse(rawMessage);
				} catch(e) {}
			}

			if (parsedJSON) {
				if (parsedJSON.type) {
					type = parsedJSON.type;
				}
				if (type === 'image') {
					// handled below
				} else {
					rawMessage = parsedJSON.content || parsedJSON.text || rawMessage;
				}
			} else {
				var cleanMsg = rawMessage.trim().replace(/\s/g, '');
				if (/^data:image\/[a-zA-Z+.-]+;base64,/i.test(cleanMsg) ||
					cleanMsg.indexOf('iVBORw0KGgo') === 0 ||
					cleanMsg.indexOf('/9j/') === 0 ||
					cleanMsg.indexOf('R0lGOD') === 0 ||
					cleanMsg.indexOf('UklGR') === 0) {
					type = 'image';
				}
			}

			if (type === 'image') {
				var imgSrc = getWhatsappImageSrc(rawMessage);
				return '<img src="' + (imgSrc || IMG_FALLBACK) + '" alt="Imagen" class="whatsapp-image" style="max-width:250px;max-height:300px;border-radius:12px;margin:4px 0;display:block;cursor:pointer;" onclick="window.open(this.src,\'_blank\')" onerror="this.src=\''+IMG_FALLBACK+'\';this.onclick=null;">' + 
				       (parsedJSON && parsedJSON.text ? '<div class="whatsapp-caption" style="margin-top:4px;font-size:13px;color:rgba(0,0,0,0.7);">' + $('<div/>').text(parsedJSON.text).html() + '</div>' : '');
			}

			if (type === 'video') {
				var mediaUrl = parsedJSON ? (parsedJSON.content || parsedJSON.url || rawMessage) : rawMessage;
				return '<a href="' + $('<div/>').text(mediaUrl).html() + '" target="_blank" class="btn btn-sm btn-primary"><i class="fas fa-video"></i> Ver Video</a>';
			}
			if (type === 'audio') {
				var mediaUrl = parsedJSON ? (parsedJSON.content || parsedJSON.url || rawMessage) : rawMessage;
				return '<audio controls style="width:100%;max-width:300px;"><source src="' + $('<div/>').text(mediaUrl).html() + '" type="audio/mpeg"></audio>';
			}
			if (type === 'document') {
				var mediaUrl = parsedJSON ? (parsedJSON.content || parsedJSON.url || rawMessage) : rawMessage;
				return '<a href="' + $('<div/>').text(mediaUrl).html() + '" target="_blank" class="btn btn-sm btn-info"><i class="fas fa-file"></i> Descargar</a>';
			}

			// Texto plano
			return $('<div/>').text(rawMessage).html();
		}

		// Función para abrir el modal de WhatsApp y cargar mensajes
		function abrirModalWhatsApp(expedienteId, clientName) {
			console.log('abriendo modal WhatsApp para expediente:', expedienteId);
			
			// Actualizar título con el nombre del cliente
			if (clientName) {
				$('#modal-whatsapp-titulo').text(clientName);
			} else {
				$('#modal-whatsapp-titulo').text('WhatsApp Hipotea');
			}

			var innerContainer = $('#whatsapp-messages-inner');
			var outerContainer = $('#whatsapp-messages-container');
			
			// Mostrar el modal
			$('#modal-whatsapp-messages').modal('show');
			
			// Guardar el expedienteId en el input oculto
			$('#whatsapp-expediente-id').val(expedienteId);
			
			// Limpiar input de mensaje
			$('#whatsapp-message-input').val('').focus();
			
			// Mostrar spinner
			innerContainer.html('<div class="text-center text-muted mt-5"><i class="fas fa-spinner fa-spin"></i> Cargando conversaciones...</div>');
			
			// Construir URL correctamente
			var baseUrl = '{{ path('api_get_whatsapp_messages', {'id': '0'}) }}';
			var url = baseUrl.replace('/0', '/' + expedienteId);
			
			// Cargar conversaciones vía AJAX
			$.ajax({
				url: url,
				type: 'GET',
				dataType: 'json',
				success: function(data) {
					innerContainer.empty();
					
					var messages = Array.isArray(data) ? data : (data.messages || []);
					console.log('Mensajes recibidos:', messages);
					
					if (!messages || messages.length === 0) {
						innerContainer.html('<div class="alert alert-info" style="background: #fff; border:none; border-radius: 7.5px; box-shadow: 0 1px 0.5px rgba(0,0,0,.13);">No hay mensajes WhatsApp para este expediente.</div>');
						return;
					}
					
					var messagesHtml = '';
					var lastDirection = null;

					messages.forEach(function(msg) {
						console.log('Procesando mensaje:', msg.message_type, '| message:', msg.message.substring(0, 100));
						
						var direction = msg.direction === 'enviado' ? 'enviado' : 'recibido';
						var hasTail = (direction !== lastDirection);
						
						// Formatear timestamp
						var timeStr = '';
						if (msg.timestamp) {
							var date = new Date(msg.timestamp);
							var hh = String(date.getHours()).padStart(2, '0');
							var mm = String(date.getMinutes()).padStart(2, '0');
							timeStr = hh + ':' + mm;
						}
						
						var messageContent = buildWhatsappMessageContent(msg);

						messagesHtml += '<div class="whatsapp-message-row ' + direction + (hasTail ? '' : ' no-tail') + '">' +
							'<div class="whatsapp-bubble ' + direction + '">' +
								'<div class="whatsapp-text">' + messageContent + '</div>' +
								'<div class="whatsapp-meta">' +
									'<span class="whatsapp-time">' + timeStr + '</span>';
						
						if (direction === 'enviado') {
							messagesHtml += '<span class="whatsapp-check"><i class="fas fa-check-double"></i></span>';
						}
						
						messagesHtml += '</div>' + // meta
							'</div>' + // bubble
						'</div>'; // row
						
						lastDirection = direction;
					});
					
					innerContainer.html(messagesHtml);
					
					// Scroll al final
					setTimeout(function() {
						outerContainer.scrollTop(outerContainer[0].scrollHeight);
					}, 100);
					
					// Iniciar polling para nuevos mensajes cada 10 segundos
					iniciarPollingMensajesWhatsApp(expedienteId);
				},
				error: function(xhr) {
					console.error('Error al cargar mensajes:', xhr);
					innerContainer.html('<div class="alert alert-danger">Error al cargar los mensajes.</div>');
				}
			});
		}
		
		// Función para enviar mensaje
		function enviarMensajeWhatsApp() {
			var expedienteId = $('#whatsapp-expediente-id').val();
			var texto = $('#whatsapp-message-input').val().trim();
			
			if (!texto) {
				console.log('Por favor escribe un mensaje');
				return;
			}
			
			if (!expedienteId) {
				console.log('Error: expediente no identificado');
				return;
			}
			
			var btn = $('#whatsapp-send-btn');
			btn.prop('disabled', true).css('opacity', '0.5');
			
			$.ajax({
				url: '{{ path('api_send_whatsapp_message') }}',
				type: 'POST',
				dataType: 'json',
				contentType: 'application/json',
				headers: {
					'X-API-KEY': '123456'
				},
				data: JSON.stringify({
					id_expediente: parseInt(expedienteId),
					text: texto
				}),
				success: function(data) {
					console.log('Mensaje enviado:', data);
					$('#whatsapp-message-input').val('');
					var clientName = $('#modal-whatsapp-titulo').text();
					abrirModalWhatsApp(expedienteId, clientName);
				},
				error: function(xhr) {
					console.error('Error al enviar:', xhr);
					var errorMsg = 'Error desconocido';
					if (xhr.responseJSON && xhr.responseJSON.error) {
						errorMsg = xhr.responseJSON.error;
					}
					console.log('Error al enviar el mensaje: ' + errorMsg);
				},
				complete: function() {
					btn.prop('disabled', false).css('opacity', '1');
				}
			});
		}
		
		$(document).on('click', '#whatsapp-send-btn', function() {
			enviarMensajeWhatsApp();
		});
		
		$(document).on('keypress', '#whatsapp-message-input', function(e) {
			if (e.which === 13) {
				enviarMensajeWhatsApp();
				return false;
			}
		});

		// Variable global para mantener el intervalo de polling
		var whatsappPollingInterval = null;
		var ultimosHashMensajes = null;
		
		// Función para iniciar polling cada 10 segundos
		function iniciarPollingMensajesWhatsApp(expedienteId) {
			// Detener polling anterior si existe
			if (whatsappPollingInterval) {
				clearInterval(whatsappPollingInterval);
			}
			
			// Iniciar nuevo polling
			whatsappPollingInterval = setInterval(function() {
				// Solo continuar si el modal está visible
				if (!$('#modal-whatsapp-messages').is(':visible')) {
					clearInterval(whatsappPollingInterval);
					whatsappPollingInterval = null;
					return;
				}
				
				var baseUrl = '{{ path('api_get_whatsapp_messages', {'id': '0'}) }}';
				var url = baseUrl.replace('/0', '/' + expedienteId);
				
				$.ajax({
					url: url,
					type: 'GET',
					dataType: 'json',
					success: function(data) {
						var innerContainer = $('#whatsapp-messages-inner');
						var outerContainer = $('#whatsapp-messages-container');
						
						var messages = Array.isArray(data) ? data : (data.messages || []);
						
						if (!messages || messages.length === 0) {
							return;
						}
						
						// Crear hash de mensajes para detectar cambios
						var hashActual = JSON.stringify(messages);
						
						// Si hay cambios, actualizar el listado
						if (hashActual !== ultimosHashMensajes) {
							ultimosHashMensajes = hashActual;
							
							var messagesHtml = '';
							var lastDirection = null;

							messages.forEach(function(msg) {
								var direction = msg.direction === 'enviado' ? 'enviado' : 'recibido';
								var hasTail = (direction !== lastDirection);
								
								// Formatear timestamp
								var timeStr = '';
								if (msg.timestamp) {
									var date = new Date(msg.timestamp);
									var hh = String(date.getHours()).padStart(2, '0');
									var mm = String(date.getMinutes()).padStart(2, '0');
									timeStr = hh + ':' + mm;
								}
								
								var messageContent = buildWhatsappMessageContent(msg);

								messagesHtml += '<div class="whatsapp-message-row ' + direction + (hasTail ? '' : ' no-tail') + '">' +
									'<div class="whatsapp-bubble ' + direction + '">' +
										'<div class="whatsapp-text">' + messageContent + '</div>' +
										'<div class="whatsapp-meta">' +
											'<span class="whatsapp-time">' + timeStr + '</span>';
								
								if (direction === 'enviado') {
									messagesHtml += '<span class="whatsapp-check"><i class="fas fa-check-double"></i></span>';
								}
								
								messagesHtml += '</div>' + // meta
									'</div>' + // bubble
								'</div>'; // row
								
								lastDirection = direction;
							});
							
							innerContainer.html(messagesHtml);
							
							// Scroll al final
							setTimeout(function() {
								outerContainer.scrollTop(outerContainer[0].scrollHeight);
							}, 100);
						}
					},
					error: function(xhr) {
						// Silenciosamente ignorar errores de polling
					}
				});
			}, 10000); // Cada 10 segundos
		}
		
		// Detener polling al cerrar el modal
		$('#modal-whatsapp-messages').on('hidden.bs.modal', function() {
			if (whatsappPollingInterval) {
				clearInterval(whatsappPollingInterval);
				whatsappPollingInterval = null;
				ultimosHashMensajes = null;
			}
		});

		$(document).ready(function() {
			// Código que necesita DOM listo
		});
	</script>
	<script src="{{ asset('assets/js/custom/lista-expedientes.min.js') }}"></script>
	<script src="{{ asset('assets/js/custom/notificacion-expediente.js') }}"></script>
	<script src="{{ asset('assets/js/custom/cancelar-expediente.js') }}"></script>
{% endblock %}
{% block body %}
	<div class="card">
		<div class="card-body">
			{% include '@App/Backoffice/MensajeAlerta.html.twig' %}
			<h4 class="card-title">{{ titulo }}</h4>
			
			<!-- Filtros -->
			<div class="card mb-3">
				<!--<div class="card-header">
					<h5 class="mb-0">Filtros de búsqueda</h5>
				</div>-->
                <div class="card-body">					
                    <form method="get" action="{{ path('lista_expedientes_paginacion') }}" id="formFiltros">
                        {# Campo oculto para preservar el límite de registros al filtrar #}
                        <input type="hidden" name="limit" value="{{ limit }}">
                        
                        <div class="row">
                            {# Filtro por número de expediente 
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') %}
                                <div class="col-md-2 mb-3">
                                    <label for="expediente">Nº Expediente</label>
                                    <input type="text" 
                                            class="form-control" 
                                            id="expediente" 
                                            name="expediente" 
                                            value="{{ app.request.get('expediente') }}"
                                            placeholder="Nº expediente">
                                </div>
                            {% endif %}#}

                            {# Filtro por clientes - Solo si no es ROLE_CLIENTE ni ROLE_COLABORADOR #}
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') %}
                                <div class="col-md-2 mb-3">
                                    <label for="clientes">Buscar cliente </label>
                                    <select class="form-control select2" 
                                            id="clientes" 
                                            name="clientes[]" 
                                            multiple="multiple">
                                        {% for cliente in clientes %}
                                            {% set usuario = cliente %}
                                            {% if usuario.apellidos|length is same as(0) %}
                                                {% if usuario.username|length is same as(0) %}
                                                    {% set nombreCompleto = '' %}
                                                {% else %}
                                                    {% set nombreCompleto = usuario.username %}
                                                {% endif %}
                                            {% else %}
                                                {% if usuario.username|length is same as(0) %}
                                                    {% set nombreCompleto = usuario.apellidos %}
                                                {% else %}
                                                    {% set nombreCompleto = usuario.apellidos ~ ', ' ~ usuario.username %}
                                                {% endif %}
                                            {% endif %}
                                            {% if nombreCompleto|length is not same as(0) %}
                                                {% set nombreCompleto = nombreCompleto  %}
                                            {% endif %}
                                            <option value="{{ cliente.idUsuario }}" 
                                                    {% if app.request.get('clientes') and cliente.idUsuario in app.request.get('clientes') %}selected{% endif %}>
                                                {{ nombreCompleto }}
                                            </option>
                                        {% endfor %}
                                    </select>
                                    
                                </div>
                            {% endif %}

                            {# Filtro por colaboradores #}
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') %}
                                <div class="col-md-2 mb-3">
                                    <label for="colaboradores">Buscar usuario colaborador</label>
                                    <select class="form-control select2" 
                                            id="colaboradores" 
                                            name="colaboradores[]" 
                                            multiple="multiple"
                                            data-placeholder="">
                                        {% for colaborador in colaboradores %}
                                            {% set usuario = colaborador %}
                                            {% if usuario.apellidos|length is same as(0) %}
                                                {% if usuario.username|length is same as(0) %}
                                                    {% set nombreCompleto = '' %}
                                                {% else %}
                                                    {% set nombreCompleto = usuario.username %}
                                                {% endif %}
                                            {% else %}
                                                {% if usuario.username|length is same as(0) %}
                                                    {% set nombreCompleto = usuario.apellidos %}
                                                {% else %}
                                                    {% set nombreCompleto = usuario.apellidos ~ ', ' ~ usuario.username %}
                                                {% endif %}
                                            {% endif %}
                                            <option value="{{ colaborador.idUsuario }}"
                                                    {% if app.request.get('colaboradores') and colaborador.idUsuario in app.request.get('colaboradores') %}selected{% endif %}>
                                                {{ nombreCompleto }}
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                            {% endif %}

                            {# Filtro por comerciales - Solo para ADMIN y COMERCIAL y TECNICO #}
                            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMERCIAL') or is_granted('ROLE_TECNICO') %}
                                <div class="col-md-2 mb-3">
                                    <label for="comerciales">Buscar comercial</label>
                                    <select class="form-control select2" 
                                            id="comerciales" 
                                            name="comerciales[]" 
                                            multiple="multiple"
                                            data-placeholder="">
                                        {% for comercial in comerciales %}
                                            <option value="{{ comercial.idUsuario }}"
                                                    {% if app.request.get('comerciales') and comercial.idUsuario in app.request.get('comerciales') %}selected{% endif %}>
                                                {{ comercial.apellidos }}, {{ comercial.nombre }}{% if comercial.username %} ({{ comercial.username }}){% endif %}
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>

                                {# Filtro por técnicos #}
                                <div class="col-md-2 mb-3">
                                    <label for="tecnicos">Buscar técnico</label>
                                    <select class="form-control select2" 
                                            id="tecnicos" 
                                            name="tecnicos[]" 
                                            multiple="multiple"
                                            data-placeholder="">
                                        {% for tecnico in tecnicos %}
                                            <option value="{{ tecnico.idUsuario }}"
                                                    {% if app.request.get('tecnicos') and tecnico.idUsuario in app.request.get('tecnicos') %}selected{% endif %}>
                                                {{ tecnico.apellidos }}, {{ tecnico.nombre }}{% if tecnico.username %} ({{ tecnico.username }}){% endif %}
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                            {% endif %}

                            {# Filtro por inmobiliarias #}
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') %}
                                <div class="col-md-2 mb-3">
                                    <label for="inmobiliarias">Buscar colaborador</label>
                                    <select class="form-control select2" 
                                            id="inmobiliarias" 
                                            name="inmobiliarias[]" 
                                            multiple="multiple">
                                            data-placeholder="Seleccionar inmobiliarias">
                                        {% for inmobiliaria in inmobiliarias %}
                                            <option value="{{ inmobiliaria.idInmobiliaria }}"
                                                    {% if app.request.get('inmobiliarias') and inmobiliaria.idInmobiliaria in app.request.get('inmobiliarias') %}selected{% endif %}>
                                                {{ inmobiliaria.nombre }}
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                            {% endif %}

                            {# Filtro por oficinas - Visible para ROLE_JEFE_OFICINA también #}
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') or is_granted('ROLE_JEFE_OFICINA') %}
                                <div class="col-md-2 mb-3">
                                    <label for="oficinas">Buscar oficina</label>
                                    <select class="form-control select2" 
                                            id="oficinas" 
                                            name="oficinas[]" 
                                            multiple="multiple"
                                            data-placeholder="">
                                        {% for oficina in oficinas %}
                                            <option value="{{ oficina.idOficina }}"
                                                    {% if (app.request.get('oficinas') and oficina.idOficina in app.request.get('oficinas')) or (not app.request.get('oficinas') and oficinaPreseleccionada is defined and oficinaPreseleccionada == oficina.idOficina) %}selected{% endif %}>
                                                {{ oficina.nombre }}
                                            </option>
                                        {% endfor %}
                                    </select>
                                </div>
                            {% endif %}
							{# Filtro de estado con botones toggle #}
							{% if not is_granted('ROLE_CLIENTE') %}
                                <div class="col-md-4 mb-3">
									<label for="estado">Estado</label>
									<div style="width: 100% !important;">
										<input type="hidden" id="estado-filtro" name="estado" value="{{ app.request.get('estado') }}">
										<button type="button" class="btn btn-sm btn-outline-danger estado-btn" data-estado="0">
											Cancelados
										</button>
										<button type="button" class="btn btn-sm btn-outline-success estado-btn" data-estado="1">
											Activos
										</button>
										<button type="button" class="btn btn-sm btn-outline-primary estado-btn" data-estado="2">
											Firmados
										</button>
									</div>
                                </div>
                            {% endif %}
                        </div>
						
						{% if not is_granted('ROLE_CLIENTE') %}
							{# Botones de acción #}
							<div class="row mt-3">
								<div class="col-12" style="text-align: center;">
									<button type="submit" class="btn btn-primary">
										<i class="fas fa-filter"></i> Filtrar
									</button>
									<a href="{{ path('lista_expedientes_paginacion') }}" class="btn btn-secondary">
										<i class="fas fa-eraser"></i> Limpiar filtros
									</a>
								</div>
							</div>
						{% endif %}
                    </form>
                </div>
			</div>
			
			{% if expedientes is defined and expedientes is iterable and expedientes|length>0 %}
				<!-- Selector de cantidad de registros y búsqueda -->
				<div class="row mb-3 align-items-center">
					<div class="col-auto">
						<label for="limitSelect" class="col-form-label">Mostrar:</label>
					</div>
					<div class="col-auto">
						<select class="form-control" id="limitSelect" onchange="cambiarLimit(this.value)">
							<option value="10" {% if limit == 10 %}selected{% endif %}>10</option>
							<option value="20" {% if limit == 20 %}selected{% endif %}>20</option>
							<option value="50" {% if limit == 50 %}selected{% endif %}>50</option>
							<option value="100" {% if limit == 100 %}selected{% endif %}>100</option>
							<option value="200" {% if limit == 200 %}selected{% endif %}>200</option>
						</select>
					</div>
					<div class="col-auto">
						<span class="col-form-label">registros</span>
					</div>
				<div class="col text-right">
					<label for="buscar-tabla" class="col-form-label mr-2">Buscar:</label>
					<input type="search" class="form-control d-inline-block" id="buscar-tabla" placeholder="" style="width: 200px;" value="{{ app.request.get('buscar') }}">
				</div>
				<div class="col-auto">
					<form method="get" id="formOrden" action="{{ path('lista_expedientes_paginacion') }}">
						{# Preservar parámetros GET actuales excepto 'orden' y 'page' #}
						{% for key, val in app.request.query.all %}
							{% if key != 'orden' and key != 'page' %}
								{% if val is iterable %}
									{% for v in val %}
										<input type="hidden" name="{{ key }}[]" value="{{ v }}">
									{% endfor %}
								{% else %}
									<input type="hidden" name="{{ key }}" value="{{ val }}">
								{% endif %}
							{% endif %}
						{% endfor %}
						<label for="ordenSelect" class="sr-only">Ordenar</label>
						<select id="ordenSelect" name="orden" class="form-control" onchange="document.getElementById('formOrden').submit()">
							<option value="fecha_modificacion" {% if app.request.get('orden') == 'fecha_modificacion' or app.request.get('orden') is null %}selected{% endif %}>Fecha modificación (por defecto)</option>
							<option value="fecha_creacion" {% if app.request.get('orden') == 'fecha_creacion' %}selected{% endif %}>Fecha creación</option>
						</select>
					</form>
				</div>


				</div>
				
				<form class="text-truncate" id="formulario-borrar-entidades" method="post">
					<div style="overflow-x: auto;">
						<table class="table table-striped table-sm full-color-table full-success-table hover-table" id="tabla-entidades">
						<thead>
							<tr>
								{% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR')  %}
									<th scope="col">
										{% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
										<div class="custom-control custom-checkbox">
											<input class="custom-control-input" id="seleccionar-todas-entidades" type="checkbox">
											<label class="custom-control-label" for="seleccionar-todas-entidades"></label>
										</div>
										{% endif %}
									</th>
								{% endif %}
								<th scope="col">Fecha</th>
								{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
									<th scope="col">Nº Exp.</th>
									<th scope="col">Referencia</th>
								{% endif %}
								{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
									<th scope="col">Origen</th>
								{% endif %}
								{% if not is_granted('ROLE_CLIENTE') %}
									<th scope="col">Cliente</th>
								{% endif %}
								{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
									<th scope="col">Email</th>
									<th scope="col">Teléfono</th>
								{% endif %}
								{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR')  %}
									<th scope="col">Inmobiliaria</th>
									<th scope="col">Oficina</th>
								{% endif %}
								<th scope="col">Acciones</th>
								<th scope="col">Inmueble</th>
								<th scope="col">Fase actual</th>
								{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR')  %}
									<th scope="col">Estado</th>
								{% endif %}
							</tr>
						</thead>
						<tbody>
							{% for expediente in expedientes %}
								<tr>
									{% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR')  %}
										<td>
											{% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
											<div class="custom-control custom-checkbox">
												<input class="custom-control-input" id="entidad[{{ expediente.idExpediente }}]" name="expediente[]" type="checkbox" value="{{ expediente.idExpediente }}">
												<label class="custom-control-label" for="entidad[{{ expediente.idExpediente }}]"></label>
											</div>
											{% endif %}
										</td>
									{% endif %}
									<td data-order="{{ expediente.fechaCreacion|date('U') }}">{{ expediente.fechaCreacion|date('d/m/Y') }}</td>
									{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
										<td>
											{{ expediente.idExpediente }}
											{% if expedientesRelacionados[expediente.idExpediente] is defined %}
												{% set idsRelacionados = expedientesRelacionados[expediente.idExpediente] | split(',') %}
												<span style="color:red;font-weight:bold;">[
													{% for idRelacionado in idsRelacionados %}
														<a target="_blank" href="{{ path('modificar_expediente', {'id': idRelacionado|trim}) }}">{{ idRelacionado|trim }}</a>
														{% if not loop.last %},
														{% endif %}
													{% endfor %}
													]</span>
											{% endif %}

										</td>
										<td>{{ expediente.referencia ?? '-' }}</td>
									{% endif %}
									{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
										<td>{{ camposPersonalizados[expediente.idExpediente]['origen'] is defined ? camposPersonalizados[expediente.idExpediente]['origen'] : '' }}</td>
									{% endif %}
									{% if not is_granted('ROLE_CLIENTE') %}
										{% if expediente.idCliente is null %}
											<td></td>
										{% else %}
											{% set usuario = expediente.idCliente %}
											{% if usuario.apellidos|length is same as(0) %}
												{% if usuario.username|length is same as(0) %}
													{% set nombreCompleto = '' %}
												{% else %}
													{% set nombreCompleto = usuario.username %}
												{% endif %}
											{% else %}
												{% if usuario.username|length is same as(0) %}
													{% set nombreCompleto = usuario.apellidos %}
												{% else %}
													{% set nombreCompleto = usuario.apellidos ~ ', ' ~ usuario.username %}
												{% endif %}
											{% endif %}
											{% if nombreCompleto|length is not same as(0) %}
												{% set nombreCompleto = nombreCompleto  %}
											{% endif %}
											<td>{{ nombreCompleto }}</td>
										{% endif %}
									{% endif %}
									{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
										<td>{{ camposPersonalizados[expediente.idExpediente]['email'] is defined ? camposPersonalizados[expediente.idExpediente]['email'] : '' }}</td>
										<td>{{ camposPersonalizados[expediente.idExpediente]['telefono'] is defined ? camposPersonalizados[expediente.idExpediente]['telefono'] : '' }}</td>
									{% endif %}
									{% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR')  %}
										<td>
											{% set inmuebles_nombres = [] %}
											{% if expediente.idCliente is not null and expediente.idCliente.idInmobiliaria is not null %}
												{% set inmuebles_nombres = inmuebles_nombres|merge([expediente.idCliente.idInmobiliaria.nombre]) %}
											{% endif %}
											{% if expediente.idComercial is not null and expediente.idComercial.idInmobiliaria is not null %}
												{% set inmuebles_nombres = inmuebles_nombres|merge([expediente.idComercial.idInmobiliaria.nombre]) %}
											{% endif %}
											{% if expediente.idTecnico is not null and expediente.idTecnico.idInmobiliaria is not null %}
												{% set inmuebles_nombres = inmuebles_nombres|merge([expediente.idTecnico.idInmobiliaria.nombre]) %}
											{% endif %}
											{% if expediente.idColaborador is not null and expediente.idColaborador.idInmobiliaria is not null %}
												{% set inmuebles_nombres = inmuebles_nombres|merge([expediente.idColaborador.idInmobiliaria.nombre]) %}
											{% endif %}
											{% set inmobiliarias_unicas = [] %}
											{% for nombre in inmuebles_nombres %}
												{% if nombre not in inmobiliarias_unicas %}
													{% set inmobiliarias_unicas = inmobiliarias_unicas|merge([nombre]) %}
												{% endif %}
											{% endfor %}
											{% if inmobiliarias_unicas|length > 0 %}
												{{ inmobiliarias_unicas|join(', ') }}
											{% endif %}
										</td>
										<td>
											{% set oficinas_nombres = [] %}
											{% if expediente.idCliente is not null and expediente.idCliente.idOficina is not null %}
												{% set oficinas_nombres = oficinas_nombres|merge([expediente.idCliente.idOficina.nombre]) %}
											{% endif %}
											{% if expediente.idComercial is not null and expediente.idComercial.idOficina is not null %}
												{% set oficinas_nombres = oficinas_nombres|merge([expediente.idComercial.idOficina.nombre]) %}
											{% endif %}
											{% if expediente.idTecnico is not null and expediente.idTecnico.idOficina is not null %}
												{% set oficinas_nombres = oficinas_nombres|merge([expediente.idTecnico.idOficina.nombre]) %}
											{% endif %}
											{% if expediente.idColaborador is not null and expediente.idColaborador.idOficina is not null %}
												{% set oficinas_nombres = oficinas_nombres|merge([expediente.idColaborador.idOficina.nombre]) %}
											{% endif %}
											{% set oficinas_unicas = [] %}
											{% for nombre in oficinas_nombres %}
												{% if nombre not in oficinas_unicas %}
													{% set oficinas_unicas = oficinas_unicas|merge([nombre]) %}
												{% endif %}
											{% endfor %}
											{% if oficinas_unicas|length > 0 %}
												{{ oficinas_unicas|join(', ') }}
											{% endif %}
										</td>
									{% endif %}
									<td>
										{% if is_granted('ROLE_CLIENTE') or is_granted('ROLE_COLABORADOR') or is_granted('ROLE_JEFE_INMOBILIARIA') or is_granted('ROLE_JEFE_OFICINA') or is_granted('ROLE_RESPONSABLE_ZONA') %}
											{% if expediente.estado is same as(1) %}
												<a class="mr-2 seguimiento" href="{{ path('seguimiento_expediente',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Seguimiento" data-trigger="hover">
													<i class="fas fa-eye"></i>
													<span>Seguimiento</span>
												</a>
												<a class="mr-2 editar" href="{{ path('modificar_expediente',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Editar" data-trigger="hover">
													<i class="far fa-edit"></i>
													<span>Editar</span>
												</a>
												<a class="mr-2 documentos" href="{{ path('agregar_modificar_documentos',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Documentos" data-trigger="hover">
													<i class="far fa-file-alt"></i>
													<span>Documentos</span>
												</a>
												<a class="mr-2 firmas" href="{{ path('lista_documentos_expediente',{'id':expediente.idExpediente,'paraFirmar':'ParaFirmar'}) }}" data-toggle="popover" data-content="Firmas" data-trigger="hover">
													<i class="fas fa-edit"></i>
													<span>Firmas</span>
												</a>
											{% endif %}
										{% else %}
											<a class="mr-2 editar" target="_blank" href="{{ path('agregar_modificar_expediente',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Editar" data-trigger="hover">
												<i class="far fa-edit"></i>
												<span>Editar</span>
											</a>
											<a class="mr-2 documentos" href="{{ path('agregar_modificar_documentos',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Documentos" data-trigger="hover">
												<i class="far fa-file-alt"></i>
												<span>Documentos</span>
											</a>
											<a class="mr-2 notificar" data-notificacion-expediente="{{ expediente.idExpediente }}" {% if expediente.idCliente is null %} data-sin-cliente {% endif %} href="javascript:void(0)" data-toggle="popover" data-content="Notificar" data-trigger="hover">
												<i class="fab fa-telegram-plane"></i>
												<span>Notificar</span>
											</a>
											<a class="mr-2 email" href="{{ path('expediente_email',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Enviar Email" data-trigger="hover">
												<i class="far fa-envelope"></i>
												<span>E-Mail</span>
											</a>
											<a class="mr-2 informe" href="{{ path('informe_expediente',{'id':expediente.idExpediente}) }}" target="_blank" data-toggle="popover" data-content="Informe" data-trigger="hover">
												<i class="fas fa-info"></i>
												<span>Informe</span>
											</a>
											<a class="mr-2 pendiente" href="{{ path('informe_pendiente_expediente',{'id':expediente.idExpediente}) }}" target="_blank" data-toggle="popover" data-content="Pendiente" data-trigger="hover">
												<i class="fas fa-tasks"></i>
												<span>Pendiente</span>
											</a>
											<a class="mr-2 registro" href="{{ path('obtener_registro_expediente',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Registro" data-trigger="hover">
												<i class="fas fa-book"></i>
												<span>Registro</span>
											</a>
											<a class="mr-2 duplicar" data-enlace-duplicar-expediente="{{ expediente.idExpediente }}" href="{{ path('duplicar_expediente',{'id':expediente.idExpediente}) }}" data-toggle="popover" data-content="Duplicar" data-trigger="hover">
												<i class="far fa-copy"></i>
												<span>Duplicar</span>
											</a>
											{% if (expediente.idComercial and expediente.idComercial.idUsuario == usuarioLogueado.idUsuario) or (expediente.idTecnico and expediente.idTecnico.idUsuario == usuarioLogueado.idUsuario) %}
												{% set wa_no_messages = (
													(expediente.hasActiveSession|default(hasActiveWhatsappByExp[expediente.idExpediente]|default(false)))
													and
													(expediente.messages_count|default(messagesCountByExp[expediente.idExpediente]|default(0)) == 0)
												) %}
												<a class="mr-2 whatsapp {% if wa_no_messages %}wa-session-no-messages{% endif %}"
												   href="javascript:void(0)"
												   onclick="abrirModalWhatsApp({{ expediente.idExpediente }}, '{{ nombreCompleto|default('Cliente')|e('js') }}')"
												   {% if wa_no_messages %}title="Sesión activa — sin mensajes" aria-label="Sesión activa — sin mensajes"{% endif %}>
													<i class="fab fa-whatsapp"></i>
													<span>WhatsApp</span>
												</a>
											{% endif %}
											<a class="mr-2 cancelar" data-cancelar-expediente="{{ expediente.idExpediente }}" {% if expediente.idCliente is null %} data-sin-cliente {% endif %} href="javascript:void(0)" data-toggle="popover" data-content="Cancelar" data-trigger="hover">
												<i class="far fa-trash-alt"></i>
												<span>Cancelar</span>
											</a>
										{% endif %}
									</td>
									<td>{{ expediente.vivienda }}</td>
									<td>
										{% set id = expediente.idFaseActual.idFase %}
										{% set numeroFase =
											id in [1,3] ? 1 :
											(id in [2,4,8] ? 2 :
												(id == 5 ? 3 :
												(id in [6,9] ? 4 :
													(id in [7,10] ? 5 : 'N/A')
												)
												)
											)
										%}
										<div class="fase-popover" data-toggle="popover" data-trigger="hover" data-content="Fase {{ numeroFase }}: {{ expediente.idFaseActual.nombre }}">
											<span class="expediente-color" style="background:{{ expediente.idFaseActual.color }}"></span>
											<span>Fase {{ numeroFase }}</span>
										</div>
									</td>
									{% if not is_granted('ROLE_CLIENTE')and not is_granted('ROLE_COLABORADOR')  %}
										{% if expediente.estado is same as(0) %}
											<td>
												Cancelado
												{% if expediente.motivoCancelacion is same as(1) %}
													por: Honorarios
												{% elseif expediente.motivoCancelacion is same as(2) %}
													por: No le mejoramos la oferta
												{% elseif expediente.motivoCancelacion is same as(3) %}
													por: Operación no viable por perfil del cliente
												{% elseif expediente.motivoCancelacion is same as(4) %}
													por: Operación no viable por tasación
												{% elseif expediente.motivoCancelacion is same as(5) %}
													por: No tienen vivienda
												{% elseif expediente.motivoCancelacion is same as(6) %}
													por: Compran después de 3 meses
												{% elseif expediente.motivoCancelacion is same as(7) %}
													por: Importe hipoteca inferior a 100.000 €
												{% elseif expediente.motivoCancelacion is same as(8) %}
													por: Lo hacen con su banco
												{% elseif expediente.motivoCancelacion is same as(9) %}
													por: Lo hacen con otra empresa intermediaria
												{% elseif expediente.motivoCancelacion is same as(10) %}
													por: Duplicado
												{% elseif expediente.motivoCancelacion is same as(11) %}
													por:
												{% endif %}
												{{ expediente.texto }}
											</td>
										{% elseif expediente.estado is same as(1) %}
											<td>Activo</td>
										{% elseif expediente.estado is same as(2) %}
											<td>Firmado</td>
										{% else %}
											<td></td>
										{% endif %}
									{% endif %}
								</tr>
							{% endfor %}
						</tbody>
					</table>
					</div>
					
					<!-- Mensaje y botón en la misma línea -->
					<div class="row align-items-center mt-3 mb-3">
						<div class="col-md-6">
							<p class="text-muted mb-0">
								Mostrando registros del {{ ((currentPage - 1) * limit) + 1 }} al {{ min(currentPage * limit, totalExpedientes) }} de un total de {{ totalExpedientes }} registros (filtrado de un total de {{ totalExpedientes }} registros)
							</p>
						</div>
						<div class="col-md-6 text-right">
                            <!-- Paginación -->
                            {% if totalPages > 1 %}
                            <nav aria-label="Paginación de expedientes" class="mt-3">
                                <ul class="pagination justify-content-end">
                                    <li class="page-item {% if currentPage == 1 %}disabled{% endif %}">
                                        <a class="page-link" href="{{ path('lista_expedientes_paginacion', app.request.query.all|merge({'page': 1})) }}">
                                            <i class="fas fa-angle-double-left"></i>
                                        </a>
                                    </li>
                                    <li class="page-item {% if currentPage == 1 %}disabled{% endif %}">
                                        <a class="page-link" href="{{ path('lista_expedientes_paginacion', app.request.query.all|merge({'page': currentPage - 1})) }}">
                                            Anterior
                                        </a>
                                    </li>
                                    
                                    {% set startPage = max(1, currentPage - 2) %}
                                    {% set endPage = min(totalPages, currentPage + 2) %}
                                    
                                    {% if startPage > 1 %}
                                        <li class="page-item disabled">
                                            <span class="page-link">...</span>
                                        </li>
                                    {% endif %}
                                    
                                    {% for page in startPage..endPage %}
                                        <li class="page-item {% if page == currentPage %}active{% endif %}">
                                            <a class="page-link" href="{{ path('lista_expedientes_paginacion', app.request.query.all|merge({'page': page})) }}">
                                                {{ page }}
                                            </a>
                                        </li>
                                    {% endfor %}
                                    
                                    {% if endPage < totalPages %}
                                        <li class="page-item disabled">
                                            <span class="page-link">...</span>
                                        </li>
                                    {% endif %}
                                    
                                    <li class="page-item {% if currentPage == totalPages %}disabled{% endif %}">
                                        <a class="page-link" href="{{ path('lista_expedientes_paginacion', app.request.query.all|merge({'page': currentPage + 1})) }}">
                                            Siguiente
                                        </a>
                                    </li>
                                    <li class="page-item {% if currentPage == totalPages %}disabled{% endif %}">
                                        <a class="page-link" href="{{ path('lista_expedientes_paginacion', app.request.query.all|merge({'page': totalPages})) }}">
                                            <i class="fas fa-angle-double-right"></i>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                            {% endif %}
							
						</div>
                        <div class="col-md-22">
                            {% if not is_granted('ROLE_CLIENTE') and not is_granted('ROLE_COLABORADOR') and not is_granted('ROLE_JEFE_INMOBILIARIA') and not is_granted('ROLE_JEFE_OFICINA') and not is_granted('ROLE_RESPONSABLE_ZONA') %}
								<button class="btn btn-success waves-effect" id="boton-borrar-entidades">Borrar marcados</button>
							{% endif %}
                        </div>
					</div>
				</form>
				
				
				
			{% else %}
				<div class="alert alert-info" role="alert">
					<span>No hay expedientes pendientes.</span>
				</div>
			{% endif %}
		</div>
	</div>
{% endblock %}
{% block modals %}
	{{ parent() }}
	{% include '@App/Backoffice/Modal/ConfirmarBorrar.html' %}
	{% include '@App/Backoffice/Modal/ConfirmarDuplicar.html' %}
	{% include '@App/Backoffice/Modal/NotificacionExpediente.html.twig' %}
	{% include '@App/Backoffice/Modal/CancelarExpediente.html.twig' %}
	
	<!-- Modal WhatsApp Messages -->
	<div aria-modal="true" aria-labelledby="modal-whatsapp-titulo" class="modal fade" id="modal-whatsapp-messages" role="dialog" tabindex="-1">
		<div class="modal-dialog modal-dialog-whatsapp" role="document">
			<div class="modal-content modal-content-whatsapp">
				<div class="modal-header modal-header-whatsapp">
					<div class="whatsapp-header-content">
						<div class="whatsapp-avatar">
							<i class="fas fa-user"></i>
						</div>
						<div class="whatsapp-header-info">
							<h5 class="modal-title" id="modal-whatsapp-titulo">
								WhatsApp Hipotea
							</h5>
						</div>
					</div>
					<button aria-label="Cerrar" class="close whatsapp-close-btn" data-dismiss="modal" type="button">
						<span aria-hidden="true">&times;</span>
					</button>
				</div>
				<div class="modal-body modal-body-whatsapp" id="whatsapp-messages-container">
					<div class="whatsapp-doodle-overlay"></div>
					<div id="whatsapp-messages-inner">
						<div class="text-center text-muted mt-5">
							<i class="fas fa-spinner fa-spin"></i> Cargando...
						</div>
					</div>
				</div>
				<div class="modal-footer modal-footer-whatsapp">
					<input type="hidden" id="whatsapp-expediente-id" value="0">
					<input type="text" id="whatsapp-message-input" class="form-control whatsapp-input" placeholder="Escribe un mensaje...">
					<button id="whatsapp-send-btn" type="button" class="btn whatsapp-send-btn">
						<i class="fas fa-paper-plane"></i>
					</button>
				</div>
			</div>
		</div>
	</div>
	
	<style>
		/* Modal WhatsApp Mejorado */
		.modal-dialog-whatsapp {
			max-width: 500px;
			margin: auto;
			height: 100vh;
			display: flex;
			align-items: center;
			justify-content: center;
		}

		.modal-content-whatsapp {
			border-radius: 0;
			border: none;
			overflow: hidden;
			height: 90vh;
			max-height: 800px;
			display: flex;
			flex-direction: column;
			box-shadow: 0 5px 40px rgba(0,0,0,0.16);
		}

		.modal-header-whatsapp {
			background: #075e54;
			color: white;
			border: none;
			padding: 12px 16px;
			height: auto;
			display: flex;
			align-items: center;
			justify-content: space-between;
			flex-shrink: 0;
			border-radius: 0;
		}

		.whatsapp-header-content {
			display: flex;
			align-items: center;
			flex: 1;
		}

		.whatsapp-avatar {
			width: 40px;
			height: 40px;
			background: rgba(255,255,255,0.2);
			border-radius: 50%;
			display: flex;
			align-items: center;
			justify-content: center;
			margin-right: 12px;
			font-size: 20px;
			color: #ddd;
		}

		.whatsapp-header-info {
			flex: 1;
		}

		.whatsapp-header-info .modal-title {
			margin: 0;
			font-size: 15px;
			font-weight: 500;
			color: white;
			line-height: 1.3;
		}

		.whatsapp-close-btn {
			color: white !important;
			opacity: 0.8 !important;
			font-size: 28px !important;
			padding: 0 !important;
			margin: 0 !important;
			line-height: 1 !important;
		}

		.whatsapp-close-btn:hover {
			opacity: 1 !important;
		}

		.modal-body-whatsapp {
			flex: 1;
			overflow-y: auto;
			background-color: #e5ddd5;
			padding: 0;
			position: relative;
		}

		#whatsapp-messages-container {
			font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
			scrollbar-width: thin;
			scrollbar-color: rgba(0,0,0,.15) transparent;
		}

		#whatsapp-messages-container::-webkit-scrollbar {
			width: 6px;
		}

		#whatsapp-messages-container::-webkit-scrollbar-thumb {
			background: rgba(0,0,0,.15);
			border-radius: 3px;
		}

		#whatsapp-messages-container::-webkit-scrollbar-thumb:hover {
			background: rgba(0,0,0,.3);
		}

		.whatsapp-doodle-overlay {
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			opacity: 0.05;
			pointer-events: none;
			background-image: url('https://user-images.githubusercontent.com/15075759/28719144-86dc0f70-73b1-11e7-911d-60d70fcded21.png');
			background-size: 400px;
		}

		#whatsapp-messages-inner {
			position: relative;
			z-index: 1;
			padding: 16px 8%;
		}

		.modal-footer-whatsapp {
			background: #fff;
			border: 1px solid #e0e0e0;
			padding: 10px 12px;
			height: auto;
			flex-shrink: 0;
			display: flex;
			gap: 8px;
			align-items: flex-end;
		}

		.whatsapp-input {
			border-radius: 20px !important;
			padding: 8px 15px !important;
			font-size: 14px !important;
			border: 1px solid #d0d0d0 !important;
			flex: 1;
		}

		.whatsapp-input:focus {
			border-color: #075e54 !important;
			box-shadow: 0 0 0 2px rgba(7, 94, 84, 0.1) !important;
		}

		.whatsapp-send-btn {
			background: #075e54;
			color: white;
			border: none;
			border-radius: 50%;
			width: 36px;
			height: 36px;
			padding: 0;
			display: flex;
			align-items: center;
			justify-content: center;
			flex-shrink: 0;
			transition: all 0.2s ease;
		}

		.whatsapp-send-btn:hover:not(:disabled) {
			background: #054d42;
			transform: scale(1.05);
		}

		.whatsapp-send-btn:disabled {
			opacity: 0.6;
			cursor: not-allowed;
		}

		/* Mensajes WhatsApp */
		.whatsapp-message-row {
			display: flex;
			margin-bottom: 2px;
			width: 100%;
			position: relative;
		}
		
		.whatsapp-message-row.enviado {
			justify-content: flex-end;
		}
		
		.whatsapp-message-row.recibido {
			justify-content: flex-start;
		}
		
		.whatsapp-bubble {
			padding: 8px 12px;
			border-radius: 18px;
			position: relative;
			max-width: 85%;
			min-width: 80px;
			box-shadow: 0 1px 0.5px rgba(0,0,0,.13);
			font-size: 14px;
			line-height: 1.4;
			color: #111;
			margin-top: 1px;
			word-wrap: break-word;
			white-space: pre-wrap;
		}
		
		/* Rabo de la burbuja */
		.whatsapp-bubble::after {
			content: "";
			position: absolute;
			top: 0;
			width: 12px;
			height: 13px;
		}

		.whatsapp-bubble.enviado {
			background-color: #dcf8c6;
			border-top-right-radius: 4px;
		}
		
		.whatsapp-bubble.enviado::after {
			right: -8px;
			background: linear-gradient(135deg, #dcf8c6 0%, #dcf8c6 50%, transparent 50%, transparent 100%);
		}
		
		.whatsapp-bubble.recibido {
			background-color: #ffffff;
			border-top-left-radius: 4px;
		}

		.whatsapp-bubble.recibido::after {
			left: -8px;
			background: linear-gradient(225deg, #ffffff 0%, #ffffff 50%, transparent 50%, transparent 100%);
		}

		.whatsapp-text {
			margin-bottom: 4px;
		}

		.whatsapp-image {
			max-width: 250px;
			max-height: 300px;
			border-radius: 12px;
			margin: 4px 0;
			display: block;
			cursor: pointer;
			transition: transform 0.2s ease;
		}

		.whatsapp-image:hover {
			transform: scale(1.02);
		}

		.whatsapp-caption {
			margin-top: 4px;
			font-size: 13px;
			color: rgba(0,0,0,0.7);
		}
		
		.whatsapp-meta {
			display: flex;
			align-items: center;
			justify-content: flex-end;
			margin-top: 2px;
			height: 16px;
			font-size: 11px;
		}

		.whatsapp-time {
			color: rgba(0,0,0,.45);
			margin-right: 4px;
		}

		.whatsapp-check {
			color: #4fc3f7;
			display: inline-flex;
			align-items: center;
		}

		/* Agrupar mensajes */
		.whatsapp-message-row.no-tail .whatsapp-bubble::after {
			display: none;
		}

		/* Responsivo */
		@media (max-width: 576px) {
			.modal-dialog-whatsapp {
				max-width: 100%;
				margin: 0;
				height: 100%;
			}

			.modal-content-whatsapp {
				height: 100vh;
				max-height: none;
				border-radius: 0;
			}

			.whatsapp-bubble {
				max-width: 90%;
				font-size: 13px;
			}
		}
	</style>
{% endblock %}