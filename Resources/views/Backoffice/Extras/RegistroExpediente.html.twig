{% extends '@App/Backoffice/base.html.twig' %}
{% block devstyles %}
	{{ parent() }}
	<link href="{{ asset('assets/node_modules/datatables/media/css/dataTables.bootstrap4.css') }}" rel="stylesheet">
{% endblock %}
{% block cloudstyles %}
	{{ parent() }}
	<link href="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.19/css/dataTables.bootstrap4.min.css" rel="stylesheet">
{% endblock %}
{% block devscripts %}
	{{ parent() }}
	<script src="{{ asset('assets/node_modules/datatables/datatables.min.js') }}"></script>
{% endblock %}
{% block cloudscripts %}
	{{ parent() }}
	<script src="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.19/js/jquery.dataTables.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.19/js/dataTables.bootstrap4.min.js"></script>
{% endblock %}
{% block customscripts %}
	{{ parent() }}
	<script src="{{ asset('assets/js/custom/registro-expediente.min.js') }}"></script>
{% endblock %}
{% block body %}
	<div class="card">
		<div class="card-body">
			{% include '@App/Backoffice/MensajeAlerta.html.twig' %}
			<h4 class="card-title">{{ titulo }}</h4>
			<div class="text-truncate mb-3">
				<table class="table table-striped table-sm full-color-table full-success-table hover-table" id="tabla-entidades">
					<thead>
					<tr>
						<th scope="col">Fecha</th>
						<th scope="col">Acción</th>
						<th scope="col">Usuario</th>
					</tr>
					</thead>
					<tbody>
					{% for log in logs %}
						<tr>
							<td data-order="{{ log.fecha|date('U') }}">{{ log.fecha|date('d/m/Y H:i:s') }}</td>
							<td>{{ log.accion }}</td>
							{% set usuario = log.idUsuario %}
							{% if usuario.apellidos|length is same as(0) %}
								{% if usuario.username|length is same as(0) %}
									{% set nombreCompleto = '' %}
								{% else %}
									{% set nombreCompleto = usuario.username %}
								{% endif %}
							{% else %}
								{% if usuario.username|length is same as(0) %}
									{% set nombreCompleto = usuario.apellidos %}
								{% else %}
									{% set nombreCompleto = usuario.apellidos ~ ', ' ~ usuario.username %}
								{% endif %}
							{% endif %}
							<td>{{ nombreCompleto }}</td>
						</tr>
					{% endfor %}
					</tbody>
				</table>
			</div>
			<a class="btn btn-success" href="{{ path('lista_expedientes') }}">Volver</a>
		</div>
	</div>
{% endblock %}